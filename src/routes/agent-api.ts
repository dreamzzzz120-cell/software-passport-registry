import { Router } from 'express';
import { createHash, randomBytes, randomUUID } from 'node:crypto';
import { sql } from 'drizzle-orm';
import { z } from 'zod';
import { requireAuth, requireRole, AuthenticatedRequest } from '../middleware/security.ts';
import { attachTenantScope, type ScopedDb } from '../middleware/tenant-scope.ts';
import { db } from '../db/index.ts';
import { evaluateVendorRisk } from '../agents/vendor-risk-agent.ts';

const passportInput = z.object({ passportId: z.string().trim().min(1).max(255) }).strict();
const softwareInput = z.object({ query: z.string().trim().min(1).max(500) }).strict();
const vendorRiskInput = z.object({ passportId: z.string().trim().min(1).max(255), staleAfterDays: z.number().int().min(1).max(3650).optional() }).strict();
const commandInput = z.object({ input: z.string().trim().min(1).max(500), context: z.object({ path: z.string().max(500).optional() }).optional() }).strict();
const apiKeyCreateInput = z.object({ name: z.string().trim().min(1).max(120), scopes: z.array(z.enum(['read', 'write', 'webhooks'])).min(1).max(3).refine((v) => new Set(v).size === v.length, 'scopes must be unique'), expiresAt: z.string().trim().min(1).max(64).optional() }).strict();
const mintInput = z.object({ name: z.string().trim().min(1).max(255), version: z.string().trim().min(1).max(255), publisher: z.string().trim().min(1).max(255), category: z.string().trim().min(1).max(120).default('software'), checksum: z.string().trim().regex(/^[a-fA-F0-9]{64}$/, 'checksum must be a SHA-256 hex digest'), releaseDate: z.string().trim().min(1).max(64).optional(), licenseType: z.string().trim().min(1).max(120).default('UNKNOWN'), clientId: z.string().trim().min(1).max(255).nullable().optional() }).strict();
const MUTATION_INTENT = /\b(delete|remove|purge|drop|wipe|erase|destroy|update|edit|modify|change|override|overwrite|set|mark|flag|resolve|close|reopen|approve|reject|revoke|certify|whitelist|blocklist|ban)\b/;

function hashApiKey(key: string) { return createHash('sha256').update(key, 'utf8').digest('hex'); }
function newApiKey() { return `spr_live_${randomBytes(32).toString('base64url')}`; }
function futureOrNull(value: string | undefined) { return value == null || (Number.isFinite(Date.parse(value)) && Date.parse(value) > Date.now()); }
function hasScope(req: AuthenticatedRequest, scope: 'read' | 'write' | 'webhooks') { const parsed = z.array(z.enum(['read', 'write', 'webhooks'])).safeParse((req as any).sprApiKeyScopes); return parsed.success && parsed.data.includes(scope); }
function requireScope(scope: 'read' | 'write' | 'webhooks') { return (req: AuthenticatedRequest, res: any, next: any) => { if (!(req as any).sprApiKeyId || hasScope(req, scope)) return next(); return res.status(403).json({ error: { code: 'API_SCOPE_REQUIRED', message: `${scope} scope is required.` } }); }; }
async function requireApiKey(req: AuthenticatedRequest, res: any, next: any) {
  const supplied = typeof req.headers['x-api-key'] === 'string' ? req.headers['x-api-key'].trim() : '';
  if (!/^spr_live_[A-Za-z0-9_-]{43}$/.test(supplied)) return res.status(401).json({ error: { code: 'INVALID_API_KEY', message: 'Missing or invalid API key.' } });
  try {
    const keyHash = hashApiKey(supplied);
    const row = (await db.execute(sql`SELECT id,tenant_id,scopes,expires_at,revoked_at FROM spr_api_keys WHERE key_hash=${keyHash} AND revoked_at IS NULL AND (expires_at IS NULL OR expires_at = '' OR expires_at::timestamptz > CURRENT_TIMESTAMP) LIMIT 1`) as any).rows?.[0];
    if (!row) return res.status(401).json({ error: { code: 'INVALID_API_KEY', message: 'Missing, expired, or revoked API key.' } });
    const scopes = z.array(z.enum(['read', 'write', 'webhooks'])).safeParse(JSON.parse(String(row.scopes ?? '[]')));
    if (!scopes.success) return res.status(401).json({ error: { code: 'INVALID_API_KEY', message: 'API key configuration is invalid.' } });
    req.user = { id: 0, uid: `api-key:${row.id}`, email: '', tenantId: String(row.tenant_id), role: 'ApiKey', clientId: null, emailVerified: true };
    req.db = await attachTenantScope(String(row.tenant_id), res);
    (req as any).sprApiKeyId = String(row.id); (req as any).sprApiKeyScopes = scopes.data;
    await db.execute(sql`UPDATE spr_api_keys SET last_used_at=CURRENT_TIMESTAMP::text WHERE id=${row.id} AND revoked_at IS NULL`);
    return next();
  } catch (error) { return next(error); }
}

export function createAgentApiRouter() {
  const router = Router();
  const apiV1 = Router();
  apiV1.use(requireApiKey);
  apiV1.get('/openapi.json', (_req, res) => res.json({ openapi: '3.1.0', info: { title: 'Software Passport Registry API', version: '1.0.0', description: 'Evidence-first software verification. UNKNOWN is valid and never represents approval.' }, servers: [{ url: '/api/agent/v1' }], security: [{ ApiKeyAuth: [] }], components: { securitySchemes: { ApiKeyAuth: { type: 'apiKey', in: 'header', name: 'X-API-Key' } } }, paths: { '/passports': { post: { summary: 'Mint/register a passport from observed identity data' } }, '/passports/verify': { post: { summary: 'Verify a passport against current evidence' } }, '/passports/{passportId}': { get: { summary: 'Retrieve a verification snapshot' } }, '/passports/{passportId}/evidence': { get: { summary: 'Retrieve observed evidence' } }, '/passports/{passportId}/freshness': { get: { summary: 'Check observation freshness' } } } }));
  apiV1.post('/passports', requireScope('write'), async (req: AuthenticatedRequest, res, next) => {
    const parsed = mintInput.safeParse(req.body); if (!parsed.success || !futureOrNull(parsed.success ? parsed.data.releaseDate : undefined)) return res.status(400).json({ error: 'INVALID_PASSPORT', details: parsed.success ? { releaseDate: 'releaseDate must be a valid date when supplied.' } : parsed.error.flatten() });
    try { const p = parsed.data; if (p.clientId) { const client = (await req.db!.execute(sql`SELECT id FROM clients WHERE tenant_id=${req.user!.tenantId} AND id=${p.clientId} LIMIT 1`) as any).rows?.[0]; if (!client) return res.status(400).json({ error: { code: 'CLIENT_NOT_FOUND', message: 'clientId must belong to the authenticated tenant.' } }); } const existing = (await req.db!.execute(sql`SELECT id,name,version,verification_status AS "verificationStatus",file_hash AS "fileHash" FROM passports WHERE tenant_id=${req.user!.tenantId} AND lower(name)=lower(${p.name}) AND version=${p.version} AND lower(file_hash)=lower(${p.checksum}) LIMIT 1`) as any).rows?.[0]; if (existing) return res.status(200).json({ created: false, passport: existing, reason: 'IDENTITY_ALREADY_REGISTERED' }); const id = `pass_${randomUUID()}`; await req.db!.execute(sql`INSERT INTO passports (id,tenant_id,client_id,name,version,publisher,category,verification_status,release_date,file_hash,license_type,ai_summary,sbom,evidence,vulnerabilities,timeline) VALUES (${id},${req.user!.tenantId},${p.clientId ?? null},${p.name},${p.version},${p.publisher},${p.category},'unverified',${p.releaseDate ?? new Date().toISOString()},${p.checksum.toLowerCase()},${p.licenseType},'', '[]','[]','[]','[]')`); return res.status(201).json({ created: true, passport: { id, name: p.name, version: p.version, publisher: p.publisher, checksum: p.checksum.toLowerCase(), verificationStatus: 'unverified', evidenceBacked: false, trustStatus: 'UNKNOWN' } }); } catch (error) { return next(error); }
  });
  apiV1.post('/passports/verify', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { const parsed = passportInput.safeParse(req.body); if (!parsed.success) return res.status(400).json({ error: 'INVALID_PASSPORT_ID', details: parsed.error.flatten() }); try { const passport = (await req.db!.execute(sql`SELECT id,name,version,overall_score,security_score,compliance_score,evidence,vulnerabilities,timeline FROM passports WHERE tenant_id=${req.user!.tenantId} AND id=${parsed.data.passportId} LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ status: 'UNKNOWN', reason: 'PASSPORT_NOT_FOUND', passportId: parsed.data.passportId }); return buildVerificationResponse(req.db!, req.user!.tenantId, passport, res, next); } catch (error) { return next(error); } });
  apiV1.get('/passports/:passportId', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { try { const passport = (await req.db!.execute(sql`SELECT id,name,version,overall_score,security_score,compliance_score,evidence,vulnerabilities,timeline FROM passports WHERE tenant_id=${req.user!.tenantId} AND id=${req.params.passportId} LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ status: 'UNKNOWN', reason: 'PASSPORT_NOT_FOUND', passportId: req.params.passportId }); return buildVerificationResponse(req.db!, req.user!.tenantId, passport, res, next); } catch (error) { return next(error); } });
  apiV1.get('/passports/:passportId/evidence', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { try { const passport = (await req.db!.execute(sql`SELECT id FROM passports WHERE tenant_id=${req.user!.tenantId} AND id=${req.params.passportId} LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ error: { code: 'PASSPORT_NOT_FOUND', message: 'Passport not found.' } }); const rows = (await req.db!.execute(sql`SELECT id,provider,control_id AS "controlId",subject,source_url AS "sourceUrl",observed_at AS "observedAt",verification_method AS "verificationMethod",status,severity,evidence_hash AS "evidenceHash",limitation FROM evidence_ledger WHERE tenant_id=${req.user!.tenantId} AND passport_id=${req.params.passportId} ORDER BY observed_at DESC LIMIT 500`) as any).rows ?? []; return res.json({ passportId: req.params.passportId, count: rows.length, evidence: rows }); } catch (error) { return next(error); } });
  apiV1.get('/passports/:passportId/freshness', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { try { const passport = (await req.db!.execute(sql`SELECT id FROM passports WHERE tenant_id=${req.user!.tenantId} AND id=${req.params.passportId} LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ error: { code: 'PASSPORT_NOT_FOUND', message: 'Passport not found.' } }); const latest = (await req.db!.execute(sql`SELECT generated_at AS "generatedAt",observation_version AS "observationVersion",canonical_payload_hash AS "canonicalPayloadHash" FROM trust_observations WHERE tenant_id=${req.user!.tenantId} AND passport_id=${req.params.passportId} ORDER BY observation_version DESC LIMIT 1`) as any).rows?.[0]; if (!latest) return res.json({ passportId: req.params.passportId, status: 'UNKNOWN', reason: 'NO_OBSERVATION' }); const staleAfterDays = Math.min(3650, Math.max(1, Number(req.query.staleAfterDays ?? 30) || 30)); const ageDays = (Date.now() - new Date(latest.generatedAt).getTime()) / 86400000; return res.json({ passportId: req.params.passportId, status: ageDays <= staleAfterDays ? 'CURRENT' : 'STALE', ageDays: Number(ageDays.toFixed(3)), staleAfterDays, generatedAt: latest.generatedAt, observationVersion: latest.observationVersion, canonicalPayloadHash: latest.canonicalPayloadHash }); } catch (error) { return next(error); } });
  apiV1.post('/verify-software', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { const parsed = softwareInput.safeParse(req.body); if (!parsed.success) return res.status(400).json({ error: 'INVALID_QUERY', details: parsed.error.flatten() }); try { const q = parsed.data.query.toLowerCase(); const passport = (await req.db!.execute(sql`SELECT id,name FROM passports WHERE tenant_id=${req.user!.tenantId} AND (LOWER(name)=${q} OR LOWER(id)=${q}) LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ status: 'UNKNOWN', reason: 'SOFTWARE_NOT_REGISTERED', query: parsed.data.query }); return buildVerificationResponse(req.db!, req.user!.tenantId, passport, res, next); } catch (error) { return next(error); } });
  apiV1.post('/vendor-risk', requireScope('read'), async (req: AuthenticatedRequest, res, next) => { const parsed = vendorRiskInput.safeParse(req.body); if (!parsed.success) return res.status(400).json({ error: 'INVALID_VENDOR_RISK_REQUEST', details: parsed.error.flatten() }); try { const tenantId = req.user!.tenantId; const passport = (await req.db!.execute(sql`SELECT id,name FROM passports WHERE tenant_id=${tenantId} AND id=${parsed.data.passportId} LIMIT 1`) as any).rows?.[0]; if (!passport) return res.status(404).json({ status: 'UNKNOWN', reason: 'PASSPORT_NOT_FOUND', passportId: parsed.data.passportId }); const findings = (await req.db!.execute(sql`SELECT id,severity,status,title,updated_at FROM trust_findings WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY updated_at DESC LIMIT 200`) as any).rows || []; const evidence = (await req.db!.execute(sql`SELECT id,provider,observed_at,verification_method,status,limitation FROM evidence_ledger WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY observed_at DESC LIMIT 500`) as any).rows || []; const latest = (await req.db!.execute(sql`SELECT generated_at,completeness_basis_points FROM trust_observations WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY observation_version DESC LIMIT 1`) as any).rows?.[0]; return res.json(evaluateVendorRisk({ passport: { id: passport.id, name: passport.name }, findings: findings.map((f: any) => ({ id: String(f.id), severity: String(f.severity || 'unknown'), status: String(f.status || 'unknown'), title: String(f.title || 'Untitled finding'), updatedAt: f.updated_at ? new Date(f.updated_at).toISOString() : null })), evidence: evidence.map((e: any) => ({ id: String(e.id), provider: e.provider == null ? null : String(e.provider), observedAt: e.observed_at ? new Date(e.observed_at).toISOString() : null, verificationMethod: e.verification_method == null ? null : String(e.verification_method), status: e.status == null ? null : String(e.status), limitation: e.limitation == null ? null : String(e.limitation) })), latestObservationAt: latest?.generated_at ? new Date(latest.generated_at).toISOString() : null, completeness: latest?.completeness_basis_points == null ? null : Number(latest.completeness_basis_points) / 10000, evaluatedAt: Date.now(), staleAfterDays: parsed.data.staleAfterDays })); } catch (error) { return next(error); } });
  router.use(apiV1);
  router.use(requireAuth);

  router.post('/api-keys', requireRole(['Owner', 'Admin']), async (req: AuthenticatedRequest, res, next) => { const parsed = apiKeyCreateInput.safeParse(req.body); if (!parsed.success || !futureOrNull(parsed.success ? parsed.data.expiresAt : undefined)) return res.status(400).json({ error: 'INVALID_API_KEY_REQUEST', details: parsed.success ? { expiresAt: 'expiresAt must be a future timestamp.' } : parsed.error.flatten() }); try { const secret = newApiKey(); const id = `key_${randomUUID()}`; const prefix = secret.slice(0, 16); await req.db!.execute(sql`INSERT INTO spr_api_keys (id,tenant_id,name,key_prefix,key_hash,scopes,expires_at,created_by) VALUES (${id},${req.user!.tenantId},${parsed.data.name},${prefix},${hashApiKey(secret)},${JSON.stringify(parsed.data.scopes)},${parsed.data.expiresAt ?? null},${String(req.user!.id)})`); return res.status(201).json({ id, name: parsed.data.name, keyPrefix: prefix, scopes: parsed.data.scopes, expiresAt: parsed.data.expiresAt ?? null, apiKey: secret, warning: 'Store this API key now. SPR cannot recover the secret after this response.' }); } catch (error) { return next(error); } });
  router.get('/api-keys', requireRole(['Owner', 'Admin']), async (req: AuthenticatedRequest, res, next) => { try { const rows = (await req.db!.execute(sql`SELECT id,name,key_prefix AS "keyPrefix",scopes,expires_at AS "expiresAt",last_used_at AS "lastUsedAt",revoked_at AS "revokedAt",created_at AS "createdAt" FROM spr_api_keys WHERE tenant_id=${req.user!.tenantId} ORDER BY created_at DESC`) as any).rows ?? []; return res.json({ keys: rows.map((r: any) => ({ ...r, scopes: z.array(z.enum(['read', 'write', 'webhooks'])).safeParse(JSON.parse(String(r.scopes || '[]'))).data ?? [] })) }); } catch (error) { return next(error); } });
  router.delete('/api-keys/:keyId', requireRole(['Owner', 'Admin']), async (req: AuthenticatedRequest, res, next) => { try { const rows = (await req.db!.execute(sql`UPDATE spr_api_keys SET revoked_at=COALESCE(revoked_at,CURRENT_TIMESTAMP::text) WHERE id=${req.params.keyId} AND tenant_id=${req.user!.tenantId} AND revoked_at IS NULL RETURNING id`) as any).rows ?? []; if (!rows.length) return res.status(404).json({ error: 'API_KEY_NOT_FOUND' }); return res.status(204).send(); } catch (error) { return next(error); } });

  router.post('/command', async (req: AuthenticatedRequest, res, next) => {
    const parsed = commandInput.safeParse(req.body); if (!parsed.success) return res.status(400).json({ error: 'INVALID_AGENT_COMMAND', details: parsed.error.flatten() });
    try { const db = req.db!; const tenantId = req.user!.tenantId; const input = parsed.data.input; const q = input.toLowerCase(); if (/what can you do|help|how do you work/.test(q)) return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'help', reply: 'I can summarize observed workspace data, inspect a passport, review vendor risk, and take you to the right workspace. Every factual result includes provenance. I do not invent evidence or silently change trust decisions.', actions: [{ label: 'Show my risk', command: 'What is my biggest risk today?' }, { label: 'Show clients', path: '/clients' }, { label: 'Show passports', path: '/passports' }, { label: 'Show vendor risk', path: '/vendors' }] }); if (MUTATION_INTENT.test(q)) return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'refused_mutation', reply: 'I can’t do that. The SPR Agent has no ability to create, change, delete, resolve or decide anything — it only reports records it can retrieve from your workspace. Use the relevant SPR workspace for changes; every change there is authorised and recorded on its own.', actions: [{ label: 'Risk summary', command: 'What is my biggest risk today?' }, { label: 'Open Passports', path: '/passports' }] }); const nav = navigationIntent(q); if (nav) return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'navigation', ...nav }); if (/biggest risk|highest risk|most risky|risk today|what should i (do|work on)|priority|priorities|overview|summary|how are we doing/.test(q)) { const summary = await getRiskSummary(db, tenantId); return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'summary', reply: summary.reply, data: summary.data, provenance: summary.provenance }); } const passportMatch = q.match(/(?:why did|explain|show|inspect|check|assess|verify)\s+(?:the\s+)?(?:passport|software)\s*[:#-]?\s*(.+)$/i) || q.match(/(?:verify|check|assess)\s+(.+)$/i); if (passportMatch?.[1]?.trim()) return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'passport', action: { type: 'verify', endpoint: '/api/agent/v1/verify-software', payload: { query: passportMatch[1].trim() } }, reply: `I’ll inspect “${passportMatch[1].trim()}” and return the observed records and their provenance. I will not invent a trust decision.` }); if (/vendor|third.?party/.test(q) && /risk|review|check|assess/.test(q)) return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'vendor_risk', path: '/vendors', reply: 'Opening Vendor Risk. Results there are based on observed evidence and findings.' }); return res.json({ schemaVersion: 'spr-experience-agent-v1', intent: 'unsupported', reply: 'I don’t have a safe wired action for that request yet. I can summarize observed risk, inspect or verify software, review vendor risk, or open an SPR workspace.', actions: [{ label: 'Risk summary', command: 'What is my biggest risk today?' }, { label: 'Open Command Center', path: '/dashboard' }, { label: 'Open Passports', path: '/passports' }] }); } catch (error) { return next(error); }
  });
  return router;
}

function navigationIntent(q: string): { path: string; reply: string } | null { const routes: Array<[RegExp, string, string]> = [[/command center|dashboard|home/, '/dashboard', 'Opening the Command Center.'], [/clients?|customer list|client management/, '/clients', 'Opening Clients.'], [/passports?|registry|software inventory/, '/passports', 'Opening Passports.'], [/vendors?|third.?party risk/, '/vendors', 'Opening Vendor Risk.'], [/monitoring|alerts?/, '/monitoring', 'Opening Monitoring.'], [/compliance|governance/, '/compliance', 'Opening Compliance.'], [/reports?/, '/reports', 'Opening Reports.'], [/billing|subscription|plan/, '/billing', 'Opening Billing.'], [/settings?/, '/settings', 'Opening Settings.']]; for (const [pattern, path, reply] of routes) if (pattern.test(q)) return { path, reply }; return null; }

async function getRiskSummary(db: ScopedDb, tenantId: string) { const [clients, passports, findings, alerts] = await Promise.all([db.execute(sql`SELECT COUNT(*)::int AS count FROM clients WHERE tenant_id=${tenantId}`), db.execute(sql`SELECT COUNT(*)::int AS count FROM passports WHERE tenant_id=${tenantId}`), db.execute(sql`SELECT COUNT(*)::int AS total, COUNT(*) FILTER (WHERE LOWER(severity) IN ('critical','high') AND LOWER(status) NOT IN ('resolved','closed','verified'))::int AS critical_high, COUNT(*) FILTER (WHERE LOWER(status) NOT IN ('resolved','closed','verified'))::int AS open FROM trust_findings WHERE tenant_id=${tenantId}`), db.execute(sql`SELECT COUNT(*) FILTER (WHERE LOWER(status) NOT IN ('resolved','closed'))::int AS active FROM alerts WHERE tenant_id=${tenantId}`)]); const top = (await db.execute(sql`SELECT p.id AS passport_id, p.name, p.client_id, COUNT(f.id)::int AS open_findings, COUNT(f.id) FILTER (WHERE LOWER(f.severity) IN ('critical','high'))::int AS critical_high, ARRAY_AGG(f.id) FILTER (WHERE f.id IS NOT NULL AND LOWER(f.status) NOT IN ('resolved','closed','verified')) AS finding_ids FROM passports p LEFT JOIN trust_findings f ON f.passport_id=p.id AND f.tenant_id=${tenantId} AND LOWER(f.status) NOT IN ('resolved','closed','verified') WHERE p.tenant_id=${tenantId} GROUP BY p.id,p.name,p.client_id ORDER BY critical_high DESC, open_findings DESC, p.name ASC LIMIT 10`) as any).rows || []; const clientRows = (await db.execute(sql`SELECT id,name FROM clients WHERE tenant_id=${tenantId} ORDER BY name ASC LIMIT 10`) as any).rows || []; const c = (clients as any).rows?.[0]?.count ?? 0; const p = (passports as any).rows?.[0]?.count ?? 0; const f = (findings as any).rows?.[0] ?? {}; const a = (alerts as any).rows?.[0]?.active ?? 0; const generatedAt = new Date().toISOString(); return { reply: `I found ${c} client(s), ${p} passport(s), ${f.open ?? 0} open finding(s), ${f.critical_high ?? 0} critical/high open finding(s), and ${a} active alert(s). These are database observations, not invented estimates.`, data: { counts: { clients: c, passports: p, openFindings: f.open ?? 0, criticalHighFindings: f.critical_high ?? 0, activeAlerts: a }, topPassports: top, topClients: clientRows }, provenance: { generatedAt, tenantScoped: true, sources: [{ table: 'clients', fields: ['id'], observation: 'COUNT(*)', filter: 'tenant_id = authenticated tenant' }, { table: 'passports', fields: ['id'], observation: 'COUNT(*)', filter: 'tenant_id = authenticated tenant' }, { table: 'trust_findings', fields: ['id', 'severity', 'status'], observation: 'open and critical/high counts plus finding IDs for ranked passports', filter: 'tenant_id = authenticated tenant' }, { table: 'alerts', fields: ['status'], observation: 'active alert count', filter: 'tenant_id = authenticated tenant' }] } }; }

async function buildVerificationResponse(db: ScopedDb, tenantId: string, passport: any, res: any, next: any) { try { const findings = (await db.execute(sql`SELECT id,control_id,title,severity,status,description,remediation,evidence_ids,updated_at,resolved_at FROM trust_findings WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY CASE severity WHEN 'critical' THEN 1 WHEN 'high' THEN 2 WHEN 'medium' THEN 3 WHEN 'low' THEN 4 ELSE 5 END, updated_at DESC`) as any).rows || []; const evidence = (await db.execute(sql`SELECT id,provider,control_id,subject,source_url,observed_at,verification_method,status,severity,evidence_hash,limitation FROM evidence_ledger WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY observed_at DESC LIMIT 200`) as any).rows || []; const observations = (await db.execute(sql`SELECT id,observation_version,generated_at,previous_observation_id,evidence_ids,finding_ids,canonical_payload_hash,completeness_basis_points,open_finding_count,unknown_dimension_count FROM trust_observations WHERE tenant_id=${tenantId} AND passport_id=${passport.id} ORDER BY observation_version DESC LIMIT 20`) as any).rows || []; const latest = observations[0]; const openFindings = findings.filter((f: any) => !['resolved','closed','verified'].includes(String(f.status).toLowerCase())); const criticalOrHigh = openFindings.filter((f: any) => ['critical','high'].includes(String(f.severity).toLowerCase())); const completeness = latest?.completeness_basis_points == null ? null : Number(latest.completeness_basis_points) / 10000; const observed = Boolean(latest || evidence.length || findings.length); return res.json({ schemaVersion: 'spr-agent-v1', status: observed ? 'OBSERVED' : 'UNKNOWN', software: { passportId: passport.id, name: passport.name }, trustDecision: { status: 'NOT_EVALUATED_BY_EXPERIENCE_AGENT', reason: 'The Experience Agent reports observed records and does not create or duplicate SPR trust decisions.' }, evidence: { count: evidence.length, completeness, latestObservationAt: latest?.generated_at ?? null, latestHash: latest?.canonical_payload_hash ?? null }, findings: { total: findings.length, open: openFindings.length, criticalOrHigh: criticalOrHigh.length, items: findings.slice(0, 50) }, verification: { observed, evidenceBacked: evidence.length > 0, generatedAt: latest?.generated_at ?? null }, sources: evidence.slice(0, 50).map((e: any) => ({ evidenceId: String(e.id), provider: e.provider, sourceUrl: e.source_url, observedAt: e.observed_at, verificationMethod: e.verification_method, evidenceHash: e.evidence_hash, limitation: e.limitation })), provenance: { tenantScoped: true, passportRecord: { table: 'passports', fields: ['id', 'name'], passportId: passport.id }, findingRecords: { table: 'trust_findings', fields: ['id', 'control_id', 'title', 'severity', 'status', 'description', 'remediation', 'evidence_ids', 'updated_at', 'resolved_at'], findingIds: findings.map((f: any) => String(f.id)) }, evidenceRecords: { table: 'evidence_ledger', fields: ['id', 'provider', 'control_id', 'subject', 'source_url', 'observed_at', 'verification_method', 'status', 'severity', 'evidence_hash', 'limitation'], evidenceIds: evidence.map((e: any) => String(e.id)) }, observationRecords: { table: 'trust_observations', fields: ['id', 'observation_version', 'generated_at', 'previous_observation_id', 'evidence_ids', 'finding_ids', 'canonical_payload_hash', 'completeness_basis_points', 'open_finding_count', 'unknown_dimension_count'], observationIds: observations.map((o: any) => String(o.id)) } }, policy: { rule: 'The Experience Agent reports only records it can retrieve from the authenticated tenant scope. Missing evidence is UNKNOWN. It does not manufacture, infer, or silently alter trust.' } }); } catch (error) { return next(error); } }
