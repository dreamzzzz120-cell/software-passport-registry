import React, { useState } from 'react';
import { ArrowRight, BarChart3, CheckCircle2, FileSearch, HandshakeIcon, Mail, SearchCheck, ShieldQuestion, Store, Tag, Workflow, XCircle } from 'lucide-react';

const observed = ['The authorized GitHub repository coordinates and requested ref','The exact commit SHA resolved or supplied for the scan','Supported manifest and lockfile paths found in the selected directory','Components and versions produced by Syft in CycloneDX JSON','Responses returned by OSV for versioned observed components','Byte-integrity of evidence persisted by SPR'];
const notClaimed = ['That every dependency, vulnerability, or source file was discoverable','That an external provider response is correct or exhaustive','That payload integrity proves the underlying evidence is semantically true','That a clean provider response proves an application has no security risk','That a scan is a certification, penetration test, or compliance attestation'];
const deliverables = [{title:'Repository identity',detail:'Provider, owner, repository, requested ref, exact commit SHA, subdirectory, and scan timestamps.'},{title:'Observed inventory',detail:'Discovered manifest and lockfile paths, plus explicit acquisition and observation limits.'},{title:'SBOM summary',detail:'Syft version, CycloneDX format, component counts, versions, digest, and integrity state.'},{title:'OSV evidence',detail:'Query and evidence counts, observed package versions, provider identifiers, findings, and timestamps.'}];

export default function PartnerProgramView(){ const [open,setOpen]=useState<string|null>(null); return <section className="space-y-6"><header><div className="flex items-center gap-2"><HandshakeIcon /><h2>Partner Program</h2></div><p>Evidence-first software verification for MSP and technology partners.</p></header><div className="grid gap-4 md:grid-cols-2">{deliverables.map((item)=><article key={item.title} className="rounded-lg border p-4"><div className="flex items-center gap-2"><FileSearch /><h3>{item.title}</h3></div><p>{item.detail}</p></article>)}</div><div className="grid gap-4 md:grid-cols-2"><div><h3>Observed evidence</h3>{observed.map(x=><div key={x}><CheckCircle2 className="inline mr-2" />{x}</div>)}</div><div><h3>Explicit limitations</h3>{notClaimed.map(x=><div key={x}><XCircle className="inline mr-2" />{x}</div>)}</div></div></section>; }
